function Slideshow({value1}) {
    
    var  info= value1.map(item => <div className={value1}> {value1} </div>);
    console.log('ho');
    return (  
        <div> 
           {info}
        
        
        
        </div>

    );
}

export default Slideshow;